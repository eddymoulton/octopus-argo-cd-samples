apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: update-manifests-helm-test
  annotations:
    argo.octopus.com/project: "update-manifests-helm"
    argo.octopus.com/environment: "test"
spec:
  project: default
  source:
    repoURL: https://github.com/eddymoulton/octopus-argo-cd-samples
    targetRevision: HEAD
    path: manifests/update-manifests-helm/test/
  destination:
    server: https://kubernetes.default.svc
    namespace: update-manifests-helm-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - Validate=false
      - CreateNamespace=true
